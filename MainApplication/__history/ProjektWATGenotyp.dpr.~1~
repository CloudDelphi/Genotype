program ProjektWATGenotyp;

uses
  System.StartUpCopy,
  FMX.Forms,
  uMainForm in 'uMainForm.pas' {Form1},
  ConnectionModule in 'DBConnection\ConnectionModule.pas' {FireDacPostgreSQLConnection: TDataModule};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
